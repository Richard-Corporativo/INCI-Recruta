```
## 1. Implementation
- [x] 1.1 **Schema Migration**: Add `requirements` column to `jobs` table (Text, nullable). <!-- uuid: schema-migration -->
- [x] 1.2 **Type Update**: Update `Job` and `JobFormData` interfaces to include `requirements`. <!-- uuid: type-update -->
- [x] 1.3 **Component**: Create `StringListEditor` component for managing lists of text items. <!-- uuid: component-create -->
- [x] 1.4 **Admin UI**: Integrate `StringListEditor` into `CreateJob` and `EditJob` for "Responsibilities" and "Requirements". <!-- uuid: admin-ui -->
- [x] 1.5 **Public UI**: Update `JobDetailsPublic` (and preview) to render `responsibilities` and `requirements` as styled lists. <!-- uuid: public-ui -->
```
