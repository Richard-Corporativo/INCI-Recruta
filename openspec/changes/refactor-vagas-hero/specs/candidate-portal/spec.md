## MODIFIED Requirements

### Requirement: Public Job Listing
**MODIFICATION**: Update the Hero section visual structure to use a centered, high-impact typographic layout.

#### Scenario: User views the refined hero
- **WHEN** a user navigates to `/vagas`
- **THEN** the system displays a centered hero section
- **AND** the main heading "Construir o Futuro." uses large typography with high contrast (semibold vs light italic)
- **AND** stats cards are displayed as a centered grid below the main text
- **AND** the background pattern (dots) remains subtle and non-distracting
