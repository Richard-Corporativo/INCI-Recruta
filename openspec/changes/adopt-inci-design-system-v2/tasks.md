## 1. Foundation & Configuration
- [x] 1.1 Update `index.html` with new CSS variables in `@layer base` and the new Tailwind config.
- [x] 1.2 Update `openspec/project.md` to include the Design System v2 rules and tokens.

## 2. Component Migration (Phased)
- [x] 2.1 Update Main Layout (Root) to use `bg-background text-foreground`.
- [x] 2.2 Update Buttons across all pages to follow `rounded-base bg-primary` pattern.
- [x] 2.3 Update Inputs and Selects to follow `rounded-md border-border` pattern.
- [x] 2.4 Update Cards and Containers to follow `rounded-lg bg-card` pattern.

## 3. Specialized Components
- [x] 3.1 Update Sidebar to use `bg-sidebar` and new sidebar tokens.
- [x] 3.2 Update Kanban columns and cards for the new aesthetic.
- [x] 3.3 Verify Dark Mode transition with new tokens.
