'use client';

// @component NotFoundPage | @tipo route | @versao 1.0.0
// > Página 404 — fallback para rotas inexistentes

import NotFound from '@src/views/public/NotFound';

export default function NotFoundPage() {
    return <NotFound />;
}
