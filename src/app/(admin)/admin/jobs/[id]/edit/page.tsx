'use client';

// @route EditJobPage | @tipo page | @versao 1.0.0
// > Edição de vaga existente — formulário pré-preenchido
// @calls EditJob — componente principal

import EditJob from '@src/views/EditJob';

export default function Page() {
    return <EditJob />;
}
