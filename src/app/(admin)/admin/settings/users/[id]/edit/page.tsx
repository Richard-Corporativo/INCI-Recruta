'use client';

// @route EditUserPage | @tipo page | @versao 1.0.0
// > Edição de usuário — role, permissões, status
// @calls EditUser — componente principal

import EditUser from '@src/views/EditUser';

export default function Page() {
    return <EditUser />;
}
